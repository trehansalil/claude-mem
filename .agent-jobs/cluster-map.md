# claude-mem plan masters (route every open PR to exactly one)

- **#3602 plan-14 Child Process Ownership** — every process the worker spawns dies with the worker. chroma-mcp/uv/python orphans, headless observer subprocess leaks, supervisor.json overwriting entries without killing, no process-group/detached kill, no orphan sweep at boot, no resource bound/recycle on a child.
- **#3603 plan-15 Worker Port & Liveness Authority** — one verified answer to "is a worker serving this port?". Ghost listeners / inherited socket handles, EADDRINUSE "refusing to start duplicate", spawn.lock with no holder liveness, PID-file ownership + start token, health probes with no AbortSignal/deadline, wedged-but-bound worker stalling hooks, "healthy but never ready", worker logging to its startup-date file (log filename latch, rotation, retention).
- **#3604 plan-16 Canonical Install Identity & Bundle Integrity** — one resolvable, dependency-complete plugin root. mtime-based plugin-root resolution, gutted/orphaned cache dirs, missing node_modules / externalized deps (zod, tree-sitter, ajv), marketplace manifest, version-mismatch recycle loops that kill the incumbent before a successor exists, doctor/repair/status disagreeing, stale cache GC, CVEs/bundle audit.
- **#3605 plan-17 Hook Wrapper Contract** — thin, cheap, fail-open on every host. Login-shell PATH prelude, plugin-root discovery pipe, stdin draining, Windows console windows / bash.exe / WSL, hook timeouts and budget, hook-failure counter causing exit 2 (blocking), Stop re-entry guard, exec-form node launchers.
- **#3606 plan-18 Observer Response Pipeline** — classify every output, never confirm a batch on failure, bound every history. Non-XML replies silently confirming batches, skip-contract mismatch, auth/quota/transport/context-overflow classification, conversationHistory unbounded / double-appended, max_tokens + finish_reason=length, budget/recycle policy, preserving rejected batches, observation output quality (invalid type, dedup, unhedged claims).
- **#3607 plan-19 Observer Subprocess Isolation** — explicit env, cwd, config, tool set, auth source for the headless generator, with loud degradation. Inherited ~/.claude config and third-party hooks, disallowedTools/SendMessage lockdown, cwd + cross-spawn cmd.exe quoting, sanitizeEnv/proxy, keychain and OAuth token resolution, findClaudeExecutable caching/ENOENT, observer transcripts polluting the user's projects, generator config knobs unreachable from settings.
- **#3608 plan-20 Project Identity & Injection Scope** — one resolver shared by capture, sync and retrieval. cwd-basename vs git-toplevel, worktrees/submodules/monorepos, case-collation, merged_into_project, excluded projects, context config that never reaches the query, injection idempotency/determinism, timeline anchoring, folder CLAUDE.md stubs.
- **#3609 plan-21 SQLite Schema Evolution & Queue State Integrity** — one DDL path guarded by introspection, immutable session keys, transitionable queue/session states. Migration ordering, table rebuilds, FK on mutable memory_session_id, retryMessage/completeByDbId dead transitions, unbounded growth/retention/VACUUM, busy_timeout, settings.json atomic writes.
- **#3610 plan-22 Chroma Sidecar Contract** — host-safe spawn with pinned deps and a clean env, single-writer upsert sync, honest fallback (or retire the sidecar). uvx spawn args/quoting, VIRTUAL_ENV/PYTHONPATH leakage, onnxruntime/chromadb pinning, add-only sync duplication and watermarks, writer lock/arbitration, executeWithFallback lying, backfill durability.
- **#3611 plan-23 Host Integration Contracts** — every non-Claude-Code adapter and the MCP/HTTP tool surface validated by contract tests against the host's real schema. OpenCode plugin export/events/port, Codex hooks + Windows command form, Antigravity CLI/IDE + transcript watcher, other harnesses (Kimi, Copilot CLI, OMP, DeepSeek, Gemini), hook-CLI platforms not starting the generator, MCP tool annotations/CORS/stdio surface.
- **#3618 plan-24 Server Runtime & Cloud Sync Contract** — one generation pipeline shared by worker and server, session linkage on every write path, bounded sync. src/server, Postgres + BullMQ, /v1/* routes, sync-hub, CloudSync/sync_outbox, server-generation prompts/parse, CLAUDE_MEM_RUNTIME=server hook paths.
- **#3982 plan-25 Search Read Path (FTS/CJK)** — CJK-aware matching when FTS5 unicode61 returns 0; search read path and result rendering, worker SQLite + server search parity.
- **#2785 plan-12 Provider & Extensibility Roadmap** — NET-NEW capabilities, not defects. New providers (Vertex, NVIDIA NIM, OrcaRouter, OpenCode/Zen, Codex provider, HelixDB, multi-key rotation), backfill/ingest, filtering knobs, i18n, telemetry, new skills, new integrations (Grok brainbeats, backups, Pro/trial/billing), viewer/UX features, eval harnesses.

Also open and NOT a plan master: **#3372** (Merge Clinic contributor guide) and **#3763** (CMEM Pro support question). Never route a PR to those.
